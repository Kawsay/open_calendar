<%= render partial: 'calendars/calendar_frame',
           locals: { current_team:  @current_team,
                     calendars:     @calendars,
                     organizations: @organizations,
                     events:        @events,
                     new_event:     @new_event,
                     new_calendar:  @new_calendar } %>
